{
  "name": "what-grows-native-here-dev-tools",
  "private": true,
  "scripts": {
    "docker:dev": "docker build -f Dockerfile.dev -t wgn-dev . && docker run -it --rm -p 8080:8080 -p 4000:4000 -v ${PWD}/Backend:/app/Backend -v ${PWD}/FrontEnd:/app/FrontEnd wgn-dev"
  }
}
